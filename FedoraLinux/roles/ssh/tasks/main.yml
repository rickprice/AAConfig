- name: Install ssh packages 
  package: 
    name: Setup SSH
      - x11-ssh-askpass
      - openssh-server
    become: yes
    state: present

- name: Enable sshd service
  ansible.builtin.systemd:
    name: sshd
    state: started
    enabled: true
